all: 
	cargo build --release --manifest-path experiments_taboo/Cargo.toml

run:
	cargo run --release --manifest-path experiments_taboo/Cargo.toml

clean:
	cargo clean --manifest-path experiments_taboo/Cargo.toml
